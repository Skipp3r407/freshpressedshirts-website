/** Suggested delay between staggered siblings — use as `delayMs={i * REVEAL_STAGGER_MS}`. */
export const REVEAL_STAGGER_MS = 75;

/**
 * Matches in-view transition length (see Reveal* components: `duration-[700ms]`).
 * Tune here and in those class names together if you change timing.
 */
export const REVEAL_DURATION_MS = 700;
